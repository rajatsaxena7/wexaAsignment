export default function Footer() {
  return (
    <footer className="border-t border-border py-8 text-center text-sm text-muted">
      <p>
        LearnPath &middot; built on CognoDB &middot; a skill-prerequisite graph, not a table of courses
      </p>
    </footer>
  );
}
